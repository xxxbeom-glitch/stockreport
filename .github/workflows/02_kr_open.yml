# [DISABLED] 국장 개장 브리핑 — Slack 자동 발송 중단 (장중 알림만 운영: kr_intraday_slack.yml)
name: "[DISABLED] 국장 개장 브리핑"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "이 workflow는 비활성화되었습니다."
          echo "Slack 자동 발송: .github/workflows/kr_intraday_slack.yml 만 사용합니다."
          echo "리포트 HTML 생성만 필요하면 로컬에서 main.py kr_during (Slack 미발송 기본)."
