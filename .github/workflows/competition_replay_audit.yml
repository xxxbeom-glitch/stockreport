name: "REPLAY · 과거 투자 검증 (deprecated)"

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow retired
        run: |
          echo "::error::This workflow was retired. Use one of:"
          echo "  - REPLAY - 새 리플레이 시작"
          echo "  - REPLAY - 진행 중 리플레이 이어서 실행"
          echo "Developers: REPLAY DEV - 수동 복구 및 테스트"
          exit 1
