# [DISABLED] 미장 개장 브리핑 — Slack 자동 발송 중단
name: "[DISABLED] 미장 개장 브리핑"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "이 workflow는 비활성화되었습니다."
          echo "Slack 자동 발송: kr_intraday_slack.yml 만 사용합니다."
